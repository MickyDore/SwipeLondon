import { library } from '@fortawesome/fontawesome-svg-core';
import {
  faBars,
  faFire,
  faHeartCrack,
  faSliders,
} from '@fortawesome/free-solid-svg-icons';

library.add(faHeartCrack, faFire, faSliders, faBars);

export default library;
